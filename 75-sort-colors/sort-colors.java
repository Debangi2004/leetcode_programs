class Solution {
    public void sortColors(int[] nums) {
        Arrays.sort(nums);
        int n=nums.length;
        //[] ans = new int[n];
        for(int i=0;i<n;i++)
        {
            System.out.println(nums[i]);
        }
    }
}